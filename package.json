{
  "name": "tankbuster",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
